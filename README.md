```rs
fn main() {
  assert_eq!("favorite languages", ["Rust", "JavaScript"]);
  assert_eq!("editor", "neovim");
  assert_eq!("operational system", "alpine linux");
  assert_eq!("javascript bad", true);
}
```
